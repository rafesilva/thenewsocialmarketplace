
  <% if notice %>
  <% end %>
  <%= notice %>
<p><h1>What do you will buy today?</h1></p>
  </br></br>
    <div class="store">
<% cache @products do %>
    <% @products.each do |product| %>
        <% cache @products do %>
    <div class="main">
  
    </div>
             </br></br>
                     <div class="pic-sec">
                         <div class="pic">
                       <%= image_tag product.image_file.url %> 
                           <% if product.image_url.nil? %>

                                <% else %>
                                <%= image_tag(product.image_url) %>

                         </div>
                         <% end %>

                         <div class="pic_info">
                                <h2>Seller:</h2>
                             <a  href="/users/<%= product.user.id %>" ><%= product.user.name %></a>
                             </br>
                             <h2><%= product.title %></h2>
                             </br>
                             <h2>Price:</h2>

                             <h3><%= number_to_currency(product.price) %></h3>
                         </div>
                         <div class="clear"></div>
                     </div>
                     <div class="media">
                    
                         <h2>Description</h2>
                             <h3><%= sanitize(product.description) %></h3>
                            
                         
                          
                             </div>
                             <% end %>

                             </div>
                            <%= button_to 'Add to Cart', line_items_path(product_id: product), class:"btn-primary" %>
                         <div class="clear"></div>
                     
                
                     <ul>
                         
                         
                   
                    <% end %>
      <% end %> 
    


</div>