
<% if notice %>
<% end %>
<%= notice %>
<div class="main">
<h1>Your Store</h1>
<div class="store">
     
     </br>
     <div><%= link_to 'New product', new_product_path, class:"btn-primary-store" %></div>
      </br>
        <% @user.products.each do |product| %>
            <div class="">
        <div class="pic-store"><%= image_tag(product.image_file.url) %> <%= image_tag(product.image_url) %>
        </div>
        </br></br></br>
           <div class="store-tile">
          <h1><strong><%= product.title %></strong></h1>
         <h2><%=  product.description %> </h2>    </div>
       
         </br>
  
      <%= link_to 'Show', product, class:"btn-primary-store" %>
      <%= link_to 'Edit', edit_product_path(product.id), class:"btn-primary-store" %>
      <%= link_to 'Destroy', user_products_destroy_path(product), method: :destroy, class:"btn-primary-store", data: { confirm: 'Are you sure?' } %>
      </br>
         

           <% end %>
       
         </div>
  </div>